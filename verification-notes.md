# Verification Notes

## OAuthと権限制御の確認

| 観点 | 実装内容 | 確認結果 |
| --- | --- | --- |
| ログイン導線 | `/` と未認証時のダッシュボードレイアウトから `getLoginUrl()` を呼び出して Manus OAuth へ遷移する構成 | 実装済み |
| 認証状態取得 | `useAuth()` により現在ユーザーを取得し、認証済みなら `/dashboard` へ遷移 | 実装済み |
| 管理者権限制御 | サーバー側は `adminProcedure`、クライアント側は `/admin` で非管理者を `/dashboard` へリダイレクト | 実装済み |
| 権限制御テスト | `server/coffeeTicket.test.ts` で一般ユーザーの管理画面API拒否と管理者承認処理を検証 | テスト通過 |

## 主要画面・状態確認チェックリスト

| 画面/機能 | 確認内容 | 結果 |
| --- | --- | --- |
| `/` | エレガントな導入画面、ログイン導線表示、認証済み時の `/dashboard` リダイレクト | 実装済み |
| `/dashboard` | 残チケット・提供中の豆・購入プラン・申請履歴を表示 | 実装済み |
| `/purchase` | プラン選択、支払い方法選択、補足メモ、申請送信、履歴表示 | 実装済み |
| `/use` | QR利用ページ、残数表示、利用ボタン、残数ゼロ時メッセージ、豆情報表示 | 実装済み |
| `/admin` | 未承認申請一覧、承認操作、豆登録更新、利用ログ、統計APIサマリー | 実装済み |
| ローディング状態 | Skeleton による読み込み表現を各主要画面で配置 | 実装済み |
| Empty状態 | 豆未登録・申請履歴なし・利用ログなし等の案内表示を配置 | 実装済み |
| バックエンド回帰 | `pnpm test` 実行で 2 test files / 7 tests がすべて成功 | テスト通過 |
| 型安全性 | `webdev_check_status` で TypeScript error 0 を確認 | 確認済み |

## 備考

現在の確認は、開発環境上での型検査、Vitest、画面プレビューの状態確認に基づく記録です。実運用前には、実際の管理者アカウントでの承認フロー、一般ユーザーでの購入申請フロー、QR掲示用URLのスマートフォン動作確認を追加で行うと、運用開始時の不確実性をさらに下げられます。
